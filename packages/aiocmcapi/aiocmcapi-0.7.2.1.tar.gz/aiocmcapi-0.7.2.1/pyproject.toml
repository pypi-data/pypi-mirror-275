[project]
name = "aiocmcapi"
version = "0.7.2.1"
authors = [
  { name="alobuzy", email="amozebus@gmail.com" },
]
description = "Async wrapper for CoinMarketCap API"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "aiohttp >= 3.9.5",
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
[tool.poetry]
name = "requeue"
version = "0.4.0"
description = "An asynchronous queue for requesting data"
license = "GPL-3.0-or-later"
readme = "README.md"
authors = ["Maximillian Strand <maxi@millian.se>"]
repository = "https://gitlab.com/deepadmax/requeue"

[tool.poetry.dependencies]
python = "^3.12"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

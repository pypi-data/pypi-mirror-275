{% extends 'base.html' %}

{% block title %}Панель администратора{% endblock %}

{% block content %}
<div class="container">
    <h2 class="mt-5">Панель администратора</h2>
    <table class="table">
        <thead>
            <tr>
                <th>Номер</th>
                <th>Пользователь</th>
                <th>Номер автомобиля</th>
                <th>Описание нарушения</th>
                <th>Статус</th>
            </tr>
        </thead>
        <tbody>
            {% for application in applications %}
            <tr>
                <td>{{ application[0] }}</td>
                <td>{{ application[1] }}</td>
                <td>{{ application[2] }}</td>
                <td>{{ application[3] }}</td>
                <td>
                    <form action="{{ url_for('change_status', application_id=application[0]) }}" method="post">
                        <select name="status" class="form-select">
                            <option value="Ожидает" {% if application[4] == 'Ожидает' %} selected {% endif %}>Ожидает</option>
                            <option value="Подтверждено" {% if application[4] == 'Подтверждено' %} selected {% endif %}>Подтверждено</option>
                            <option value="Отклонено" {% if application[4] == 'Отклонено' %} selected {% endif %}>Отклонено</option>
                        </select>
                        <button type="submit" class="btn btn-sm btn-primary">Сохранить</button>
                    </form>
                </td>
            </tr>
            {% endfor %}
        </tbody>
    </table>
</div>
{% endblock %}

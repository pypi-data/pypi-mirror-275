from setuptools import setup

setup(name='reb_probability',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['reb_probability'],
      author = 'Roy Berg',
      zip_safe=False)

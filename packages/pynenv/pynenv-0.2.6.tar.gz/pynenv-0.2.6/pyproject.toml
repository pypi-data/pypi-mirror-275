[tool.poetry]
name = "pynenv"
version = "0.2.6"
description = "General helper functions for environments"
authors = ["Nik Sheridan <niksheridan@duck.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
loguru = "^0.7.2"
pyjson = "^1.4.1"
pyaml = "^24.4.0"


[tool.poetry.group.test.dependencies]
python-dotenv = "^1.0.1"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

from setuptools import setup, find_packages

setup(
    name='aigic',
    version='1.0.2',
    description='aigic python SDK',
    packages=find_packages(),
    author='',
    author_email='',
    install_requires=[
        'http.client'
        # ... other dependencies ...
    ]
)

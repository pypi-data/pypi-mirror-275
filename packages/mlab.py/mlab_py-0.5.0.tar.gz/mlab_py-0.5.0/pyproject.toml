[tool.poetry]
name = "mlab.py"
version = "0.5.0"
description = "Python packages for mlab ecosystem"
authors = ["Kelvin Nketia-Achiampong <achiampongk22@gmail.com>"]
readme = "README.md"
packages = [
    { include = "pymlab", from = "." },
]

[tool.poetry.dependencies]
python = "^3.8"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
